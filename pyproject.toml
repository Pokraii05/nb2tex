[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nb2tex"
version = "0.1.0"
dependencies = [
    "pypandoc",
    "beautifulsoup4"
]

[project.scripts]
nb2tex = "nb2tex.cli:main"

[tool.setuptools.packages.find]
include = ["nb2tex*"]